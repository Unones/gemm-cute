[project]
name = "gemm-cute"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Unones", email = "229073236+Unones@users.noreply.github.com" }
]
requires-python = ">=3.14.7"
dependencies = [
    "matplotlib>=3.11.1",
    "nvidia-cutlass-dsl>=4.7.0",
    "pytest>=9.1.1",
    "torch>=2.13.0",
    "triton>=3.7.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src"]
sources = ["src"]
